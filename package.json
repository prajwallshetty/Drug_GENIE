{
  "name": "drug-genie",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd my-app && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../my-app && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
